class newUsersClass {
    constructor(userId, firstName, lastName, email, passWord ) {
      this.userId = userId;
      this.firstName = firstName;
      this.lastName = lastName;   
  
    }
  }
  export default newUsersClass;